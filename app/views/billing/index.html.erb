<div class="container">
  <div class="row">
    <div class="col-md-12 mt-4 text-center">
      <h1> Welcome  <%= @user %></h1>
    </div>
  </div>
  <div class="row">
  <div> Your card :- 
    <%#= "xxxx xxxx xxxx" + Stripe::Customer.list_sources(User.last.stripe_id).data.first.last4 %>
<br><%#= Stripe::Customer.list_sources(User.last.stripe_id).data.first.brand %>
 
  </div>
   
    <h2 class="col-md-12 mt-5 text-center">
    <%= form_tag create_payment_method_path, id: "billing-create-payment-method" do  %>
        <%= link_to "Add card", add_payment_method_path, remote: true, class: "btn btn-primary" %>
      <% end %>
    </h2>
  </div>
</div>

